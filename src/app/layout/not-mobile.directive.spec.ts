import { NotMobileDirective } from './not-mobile.directive';

describe('NotMobileDirective', () => {
  /*it('should create an instance', () => {
    const directive = new NotMobileDirective();
    expect(directive).toBeTruthy();
  });*/
});
