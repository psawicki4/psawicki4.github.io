import { FlexColDirective } from './flex-col.directive';

describe('FlexColDirective', () => {
 /* it('should create an instance', () => {
    const directive = new FlexColDirective();
    expect(directive).toBeTruthy();
  });*/
});
